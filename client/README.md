# Prochat NPM package

This package contains the proto types that are used for interacting with the Prochat API.

## Release
Simply tag your commit (e.g. v1.0.0) and it will be pushed with that version.

> The `package.json` version is automatically bumped in CI and does not need to be modified.